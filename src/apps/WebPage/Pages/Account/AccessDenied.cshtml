@page
@model WebPage.Pages.Account.AccessDenied
@{
    ViewData["Title"] = "Access Denied";
}

<h1 class="error  text-danger"> @ViewData["Title"]</h1>

<div>
    You don't have access to <span class="text-danger"> @Model.ReturnUrl</span>
</div>